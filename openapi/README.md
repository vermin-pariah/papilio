# Papilio API Specifications

This directory contains the official API documentation for the Papilio project.

## 📄 Files

- **`openapi.json`**: The OpenAPI 3.0.0 specification for the REST API. You can import this into Postman, Swagger UI, or other API clients.
- **`error_codes.json`**: The global dictionary of error codes and messages used by the backend and frontend.

## ⚠️ Maintenance

According to the project's technical writing standards:
- This directory is the **single source of truth** for API contracts.
- Changes to API paths or parameters must be reflected here first.
- Only the `technical-writer` role is authorized to modify these files.
