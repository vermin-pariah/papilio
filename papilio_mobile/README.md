# Papilio Mobile

The official mobile application for the Papilio music center, built with Flutter.

## ✨ Features

- **Material 3 Design**: Clean, modern interface with adaptive themes.
- **Offline Mode**: Download your favorite tracks and listen anywhere.
- **AI-Powered Lyrics**: Synchronized, word-by-word lyrics powered by AI.
- **High-Fidelity Playback**: Native-quality audio streaming.
- **Cross-Device Sync**: Your playback progress and favorites are always in sync.

## 🛠 Prerequisites

- Flutter SDK (>= 3.19.x)
- Dart SDK
- Android Studio / Xcode (for platform-specific builds)

## 🚀 Getting Started

1.  Clone the repository.
2.  Install dependencies:
    ```bash
    flutter pub get
    ```
3.  Run the app in debug mode:
    ```bash
    flutter run
    ```

## 📦 Building for Release

### Android
```bash
flutter build apk --release
```

### iOS
```bash
flutter build ios --release
```
